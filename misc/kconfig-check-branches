#!/bin/bash -eu

here=$(dirname "${0}")

while IFS= read -r branch ; do
	branch=${branch##* }
    echo
    echo "-- ${branch} --"
	git switch "${branch}" >/dev/null
	"${here}"/kconfig-check-branch
done < <(git branch | grep -E '\*? +kconfig_')
